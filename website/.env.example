# Set these in the Vercel project (Settings -> Environment Variables).
# Must match the bot's PAYMENT_SECRET (stored in Secret Manager).
PAYMENT_SECRET=
# The bot's payment callback (defaults to the prod Cloud Function if unset).
BOT_CALLBACK_URL=https://gepetel-qvhatkgsaq-ey.a.run.app/payment/callback
