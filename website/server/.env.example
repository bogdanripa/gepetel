# Must match the bot's PAYMENT_SECRET (stored in Secret Manager) so the
# bot's /payment/callback accepts requests from this site.
PAYMENT_SECRET=

# The bot's payment callback. Defaults to the prod Cloud Function if unset.
BOT_CALLBACK_URL=https://gepetel-qvhatkgsaq-ey.a.run.app/payment/callback

# Listen port (default 8080).
PORT=8080
